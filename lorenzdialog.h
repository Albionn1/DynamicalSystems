#ifndef LORENZDIALOG_H
#define LORENZDIALOG_H

#include <QDialog>

class LorenzDialog : public QDialog {
    Q_OBJECT
public:
    explicit LorenzDialog(QWidget* parent = nullptr);
};

#endif // LORENZDIALOG_H
